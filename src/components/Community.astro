---
import { siteConfig } from "../config";
const hasCommunity = (siteConfig as any).community && (siteConfig as any).community.length > 0;
const community = (siteConfig as any).community || [];
---

{hasCommunity && (
  <div class="w-full h-2 bar-shimmer"></div>

  <section
    id="community"
    class="py-12 md:py-16"
    style="background-color: white"
  >
    <div class="max-w-6xl mx-auto px-6 lg:px-8">
      <div class="mb-10 reveal-left">
        <h2
          class="text-3xl md:text-4xl font-bold"
          style={`color: ${siteConfig.accentColor}; font-family: 'Cormorant Garamond', serif;`}
        >
          Community & Outreach
        </h2>
        <div
          class="w-16 h-1 mt-3 rounded-full"
          style={`background-color: ${siteConfig.colors.accent}`}
        ></div>
      </div>

      <div class="grid grid-cols-1 md:grid-cols-2 gap-4 md:gap-6">
        {community.map((item: any, index: number) => (
          <div
            class={`reveal stagger-${Math.min(index + 1, 6)} card-hover p-5 rounded-xl flex gap-4`}
            style={`background-color: ${siteConfig.colors.surfaceLight}; border: 1px solid ${siteConfig.colors.primary}15`}
          >
            <div
              class="w-12 h-12 rounded-full flex items-center justify-center flex-shrink-0"
              style={`background-color: ${siteConfig.colors.accent}15`}
            >
              <svg
                class="w-6 h-6"
                fill="none"
                stroke="currentColor"
                viewBox="0 0 24 24"
                style={`color: ${siteConfig.colors.accent}`}
              >
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M4.318 6.318a4.5 4.5 0 000 6.364L12 20.364l7.682-7.682a4.5 4.5 0 00-6.364-6.364L12 7.636l-1.318-1.318a4.5 4.5 0 00-6.364 0z"></path>
              </svg>
            </div>
            <div>
              <h3
                class="text-base font-semibold"
                style={`color: ${siteConfig.accentColor}`}
              >
                {item.title}
              </h3>
              <p
                class="text-sm font-medium"
                style={`color: ${siteConfig.colors.accent}`}
              >
                {item.org} · {item.period}
              </p>
              <p class="text-sm text-gray-600 mt-1">
                {item.description}
              </p>
            </div>
          </div>
        ))}
      </div>
    </div>
  </section>
)}
